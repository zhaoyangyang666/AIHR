interface:
  display_name: "AIHR"
  short_description: "Repo-specific patterns and workflows for AIHR"
  default_prompt: "Use the AIHR repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true